syntax = "proto3";
option java_outer_classname = "StudentPOJO";//生成的外部类名，同样也是文件名称
message Student {//在StudentPOJO这个外部类里面生成一个内部类，这个类名就是Student，也就是用来传递的pojo
  int32 id = 1;//1代表的是属性序号，不是值
  string name = 2;
}
